// reports.js — Reports module
// Shared by SalesPerProduct and SalesPerCategory views

/**
 * Triggers the browser's native print dialog for the current report page.
 */
function printReport() {
    window.print();
}
